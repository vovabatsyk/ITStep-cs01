using System;


namespace University
{
    public enum Gender : byte { Male, Female }
    public enum Education { Bachelor, Specialist, Master }
    public enum Frequency { Weekly, Monthly, Yearly }
}